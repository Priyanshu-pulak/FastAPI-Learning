[project]
name = "fast-api-learning"
version = "0.2.0"
description = "A modular FastAPI backend learning project"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard-no-fastapi-cloud-cli]>=0.139.2",
]

[tool.fastapi]
entrypoint = "main:app"